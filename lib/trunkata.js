// TODO: Use a binary search module
function trunkata(node, options) {
  // Fallback options
  options = options || {};

  console.log(node.height, '5');

  // Determine the line height of the node and current height
  return node;
}

// Export trunkata
module.exports = trunkata;